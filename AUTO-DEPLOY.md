# 🚀 Автоматическое обновление на GitHub

Инструкция по настройке автоматической загрузки изменений на GitHub.

## 📋 Варианты автоматизации

### Вариант 1: Простой скрипт (Рекомендуется для начала)

#### Windows:
1. Дважды кликните на `auto-push.bat`
2. Скрипт автоматически:
   - Добавит все изменения
   - Создаст коммит
   - Загрузит на GitHub

#### Linux/Mac:
1. Сделайте скрипт исполняемым:
   ```bash
   chmod +x auto-push.sh
   ```
2. Запустите:
   ```bash
   ./auto-push.sh
   ```

### Вариант 2: Автоматическое отслеживание изменений (Продвинутый)

#### Требования:
- Node.js установлен
- npm установлен

#### Установка:
```bash
npm install
```

#### Запуск:
```bash
npm run watch
```

Скрипт будет автоматически отслеживать изменения в файлах и загружать их на GitHub.

**Остановка:** Нажмите `Ctrl+C`

### Вариант 3: Простой Node.js скрипт

```bash
node auto-push-simple.js
```

## ⚙️ Настройка Git (первый раз)

Если еще не настроен Git:

1. **Установите Git:**
   - Windows: https://git-scm.com/download/win
   - Linux: `sudo apt-get install git`
   - Mac: `brew install git`

2. **Настройте Git:**
   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "ваш@email.com"
   ```

3. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Создайте новый репозиторий
   - Скопируйте URL репозитория

4. **Настройте remote:**
   ```bash
   git init
   git remote add origin https://github.com/ramay1243/gravity-flip.git
   git branch -M main
   ```
   
   **Или используйте готовый скрипт:**
   - Windows: `setup-git.bat`
   - Linux/Mac: `./setup-git.sh`

5. **Первый коммит:**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   ```

## 🔄 GitHub Actions (Автоматический деплой)

GitHub Actions уже настроен в `.github/workflows/auto-deploy.yml`

**Как это работает:**
1. Вы загружаете изменения на GitHub (через скрипт или вручную)
2. GitHub Actions автоматически деплоит на GitHub Pages
3. Сайт обновляется через 1-2 минуты

**Включение GitHub Pages:**
1. Откройте репозиторий на GitHub
2. Settings → Pages
3. Source: `Deploy from a branch`
4. Branch: `main` → `/ (root)`
5. Save

## 📝 Быстрый старт

### Windows:
1. Отредактируйте файлы
2. Дважды кликните `auto-push.bat`
3. Готово!

### Linux/Mac:
1. Отредактируйте файлы
2. Запустите `./auto-push.sh`
3. Готово!

### С автоматическим отслеживанием:
1. Запустите `npm run watch`
2. Редактируйте файлы
3. Изменения загрузятся автоматически через 2 секунды

## 🛠️ Решение проблем

### Ошибка: "Git не установлен"
- Установите Git с https://git-scm.com/

### Ошибка: "Не удалось загрузить на GitHub"
- Проверьте: `git remote -v` (должен быть настроен origin)
- Проверьте доступ к репозиторию
- Убедитесь, что branch называется `main`

### Ошибка: "Permission denied"
- Настройте SSH ключи или используйте HTTPS с токеном

### Скрипт не запускается
- Windows: Запустите от имени администратора
- Linux/Mac: `chmod +x auto-push.sh`

## 💡 Советы

1. **Используйте автоматическое отслеживание** для удобства
2. **Проверяйте изменения** перед коммитом: `git status`
3. **Делайте бэкапы** важных изменений
4. **Используйте описательные коммиты** для важных изменений

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль на ошибки
2. Убедитесь, что Git настроен правильно
3. Проверьте доступ к GitHub репозиторию

---

**Удачи с автоматизацией! 🚀**

