# 🚀 Быстрая настройка для вашего репозитория

Ваш репозиторий: **https://github.com/ramay1243/gravity-flip**

## ⚡ Быстрый старт

### 1. Первая настройка (один раз)

#### Windows:
1. Дважды кликните `setup-git.bat`
2. Готово!

#### Linux/Mac:
```bash
chmod +x setup-git.sh
./setup-git.sh
```

### 2. Автоматическая загрузка изменений

#### Вариант A: Простой (Windows)
- Дважды кликните `auto-push.bat` после изменений

#### Вариант B: Автоматическое отслеживание
```bash
npm install
npm run watch
```
Теперь просто редактируйте файлы - они загрузятся автоматически!

#### Вариант C: Linux/Mac
```bash
chmod +x auto-push.sh
./auto-push.sh
```

## 📋 Что происходит автоматически

1. ✅ Вы редактируете файлы локально
2. ✅ Запускаете скрипт (или он работает автоматически)
3. ✅ Изменения загружаются на GitHub
4. ✅ GitHub Actions автоматически деплоит на GitHub Pages
5. ✅ Сайт обновляется через 1-2 минуты

## 🔗 Ваши ссылки

- **Репозиторий:** https://github.com/ramay1243/gravity-flip
- **GitHub Pages:** https://ramay1243.github.io/gravity-flip/ (после настройки Pages)

## ⚙️ Настройка GitHub Pages

1. Откройте https://github.com/ramay1243/gravity-flip/settings/pages
2. Source: `Deploy from a branch`
3. Branch: `main` → `/ (root)`
4. Save

## 🎯 Использование

### После каждого изменения:

**Windows:**
```
Дважды кликните auto-push.bat
```

**Linux/Mac:**
```bash
./auto-push.sh
```

**С автоматическим отслеживанием:**
```bash
npm run watch
# Теперь просто редактируйте файлы!
```

## 📝 Проверка статуса

```bash
git status          # Проверить изменения
git remote -v       # Проверить remote
git log --oneline   # История коммитов
```

## 🆘 Решение проблем

### "Remote не настроен"
Запустите `setup-git.bat` (Windows) или `./setup-git.sh` (Linux/Mac)

### "Permission denied"
- Windows: Запустите от имени администратора
- Linux/Mac: `chmod +x auto-push.sh`

### "Nothing to commit"
Нет изменений для загрузки - это нормально!

---

**Готово! Теперь ваши изменения автоматически загружаются на GitHub! 🎉**

